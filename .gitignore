# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
e2e-screenshots/
playwright-report/
test-results/
*.local

# Icon reference tracer drafts/previews — never committed as icon sources (docs/art-style.md).
scripts/art/trace-out/

# Source-driven icon generation inbox: raw image-model generations + Stage-2 previews. Only the
# ingested compact source (scripts/art/icon-sources/) is committed (docs/icon-gen.md).
scripts/art/icon-gen-inbox/

# Source-driven combat sprite generation (#264), same contract as the icon inbox above: the raw
# generation and the Stage-2 preview stay local, and only the ingested compact source
# (scripts/art/sprite-sources/) is committed.
scripts/art/sprite-gen-inbox/
scripts/art/sprite-gen-out/

# Source-driven backdrop generation (#305): raw generations and review previews are inputs only.
scripts/art/backdrop-gen-inbox/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
